# UI Workspace

`ui/web`
- Main React/Vite product UI.
- Desktop is the primary design target.
- Mobile support follows a reflow strategy rather than a simple scaled-down desktop layout.

`ui/ios`
- iPhone-focused layer for future mobile-specific navigation, spacing, safe-area handling, and interaction polish.
- It should stay aligned with the web product model, not fork business logic.
