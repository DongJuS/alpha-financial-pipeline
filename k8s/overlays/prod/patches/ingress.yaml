# prod: 프로덕션 ingress 호스트
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: alpha-trading
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  rules:
    - host: alpha.prod.local
