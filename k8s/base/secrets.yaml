# 앱 시크릿만 정의. 인프라(postgres/redis/minio) 시크릿은 Bitnami Helm이 관리.
# 실제 값은 overlay에서 kustomize secretGenerator로 주입하거나
# kubectl create secret으로 별도 생성합니다.
---
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
type: Opaque
stringData:
  # DB/Redis URL — Bitnami release명에 맞춤
  DATABASE_URL: postgresql://alpha_user:CHANGE_ME@alpha-pg-postgresql:5432/alpha_db
  REDIS_URL: redis://alpha-redis-redis-master:6379/0
  JWT_SECRET: CHANGE_ME
  ANTHROPIC_API_KEY: CHANGE_ME
  OPENAI_API_KEY: CHANGE_ME
  KIS_PAPER_APP_KEY: CHANGE_ME
  KIS_PAPER_APP_SECRET: CHANGE_ME
  KIS_PAPER_ACCOUNT_NUMBER: CHANGE_ME
