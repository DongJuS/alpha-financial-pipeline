# .github Guide

이 폴더는 GitHub 관련 운영 지침만 따로 모아두는 공간입니다.

읽는 순서:

1. 이 문서를 먼저 읽는다.
2. 실제 `git push` 절차는 `GIT_PUSH.md`를 따른다.

원칙:

- 루트 `CLAUDE.md`에는 짧은 진입 안내만 둔다.
- GitHub 관련 상세 절차는 `.github/` 안에서 관리한다.
- 토큰, remote, push 같은 민감 절차는 별도 상세 문서에 둔다.
